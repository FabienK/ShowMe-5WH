COMFYUI_HOST=127.0.0.1
COMFYUI_PORT=8188
COMFYUI_TIMEOUT_SECONDS=300
COMFYUI_POLL_INTERVAL_SECONDS=2
CORS_ORIGINS=http://localhost:5173

# Génération via l'API OpenAI (GPT Image 2) — optionnel, laisser vide pour
# désactiver cette option de génération (erreur explicite si sélectionnée
# sans clé, voir services/openai_generator.py).
OPENAI_API_KEY=
